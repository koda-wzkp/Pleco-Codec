{
  "name": "pleco-codec-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "CODEC — membership/club programs on client-owned rails. Monorepo: packages/codec (the engine) + apps/* (marketing site and per-client host apps).",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
